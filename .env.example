# Anthropic API credentials.
# Copy this file to .env and fill in a real value. Never commit .env.

# API key from console.anthropic.com, used to call the Claude models
# for classification (cheap model) and routing escalation (larger model).
ANTHROPIC_API_KEY=
